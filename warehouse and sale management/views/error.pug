doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")
        title Error Page
    body
        nav.navbar.bg-primary.navbar-dark
            .container-fluid
                .row.w-100.d-flex.align-items-center
                    .col
                        a.navbar-brand(href="/") Home Page
                        a.navbar-brand(href="/data-queries") Query Pages
                    .col.text-center
                        h1.text-white Error Panel
                    .col 
        .container.d-flex.justify-content-center.min-vh-100
            .text-center.mt-5
                h1=message

        script.
            // Eğer hata mesajı varsa, alert ile göster
            if ('#{message}') {
                alert('#{message}');
            }

  
